<p id="notice"><%= notice %></p>

<div class="address">アドレス帳</div>
<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>住所</th>
      <th>年齢</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.address %></td>
        <td id="a_area"></td>
        <input type="hidden" id="aaa" value="<%= user.age %>">
        <%= javascript_pack_tag 'new' %>
        
        <script>
          document.getElementById('a_area').innerHTML = ageage + "歳" ;
        </script>
        <td><%= link_to '詳細', user %></td>
        <td><%= link_to '編集', edit_user_path(user) %></td>
        <td><%= link_to '削除', user, method: :delete, data: { confirm: '本当に削除しますか?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規登録', new_user_path %>
